<!doctype html >
<html lang="{{ ring.locale }}">
    {% include 'partials/head' %}
<body class="{{ class | join: " " }} page">
    <main class="stack">
        <h1 class="page-title">{{ ring.title }}</h1>
        <h2 class="page-subtitle">{{ title }}</h2>
        {{ content }}
    </main>
    {% include 'partials/footer' %}
</body>
</html>